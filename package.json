{
    "scripts": {
      "heroku-prebuild": "cd backend && npm cache clean --force && npm install",
      "start": "cd backend && npm start"
      //"heroku-postbuild": "cd backend && npm run build"
    }
  }